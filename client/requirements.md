## Packages
recharts | Data visualization for the dashboard
date-fns | Formatting dates nicely in tables
lucide-react | Icons for the UI

## Notes
- We use a premium dark theme.
- Roles are: 'super_admin', 'location_admin', 'manager', 'kitchen_staff'.
- Menu API requires `locationId` in the path: `/api/locations/:locationId/menu-items`.
- Super Admins must select a location to view/edit menu items.
